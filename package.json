{
	"name": "jira-planning-poker-backend",
	"version": "0.0.1",
	"description": "The backend for the Jira Planning Poker app.",
	"main": "dist/index.js",
	"scripts": {
		"dev": "bun --watch src/index.ts",
		"build": "bun build --target=bun src/index.ts --outfile=dist/index.js  --minify",
		"start": "bun dist/index.js"
	},
	"license": "ISC",
	"dependencies": {
		"socket.io": "^4.7.2"
	},
	"devDependencies": {
		"@types/socket.io": "^3.0.2"
	}
}
